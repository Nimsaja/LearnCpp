/*This is start of the header guard (will prevent a given heade file from being #included more than once
 * in the same file). ADD_H can be any unique name. By convention, we use the name of the header file.
 */
#ifndef ADD_H
#define ADD_H

//This is the content of the .h file, which is where the declarations go
int add(int x, int y); //function prototype

//This is the end of the header guard
#endif